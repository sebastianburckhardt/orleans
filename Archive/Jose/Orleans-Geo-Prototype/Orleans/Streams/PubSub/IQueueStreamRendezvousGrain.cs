#if !DISABLE_STREAMS

using System.Collections.Generic;
using System.Threading.Tasks;

namespace Orleans.Streams
{
    //internal interface IQueueStreamRendezvousGrain : IGrain
    //{
    //    //Task<Dictionary<QueueId, StreamQueueData>> UpdateQueueProducer(IRingRange producerRange, ProducerId producerId, IStreamProducerExtension streamProducer);

    //    Task RegisterConsumer(StreamId streamId, ConsumerId consumerId, IStreamConsumerExtension streamConsumer);

    //    Task UnregisterConsumer(StreamId streamId, ConsumerId consumerId);

    //    Task<int> ProducerCount(StreamId streamId);

    //    Task<int> ConsumerCount(StreamId streamId);
    //}
}

#endif